/**
 * Minimal Next.js config placeholder to satisfy task T15.
 * The app is not built/run in tests; presence and basic export are sufficient.
 */
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    appDir: true,
  },
};

export default nextConfig;

